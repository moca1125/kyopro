// 2024/03/14
// WA
#include <bits/stdc++.h>
using namespace std;

// 会議の開始時間を全探索する
// 開始時刻として考えるべきは，0,1,...23の24通り(会議は1時間だから)
// 開始時刻を世界標準時でt字であるとすると，拠点iの？？？？が会議に参加できる条件は
// 9<=(t+X(i))mod24<18である
//(9-t)mod24<=X(i)<(18-t)mod24と変形できる？？
int main()
{
    int n;
    cin >> n;
    vector<int> cnt(24);
    for ()
    {
    }
}
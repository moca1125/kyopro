// 2024/02/07

#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n;
    cin n;

    int i, j;
    vector<char> s(n);
    vector<int> count(n) = 0;
    int ans;

    for (i = 0; i < n; i++)
    {
        cin >> s.at(i);
        if (s.at(i) == 'x')
        {
            count.at(i)++;
        }
    }

    ans = 101;
    for (j = 0; j < n; j++)
    {
        for (i = 0; i < n; i++)
        {
            if (ans >)
            {
            }
        }
    }
}
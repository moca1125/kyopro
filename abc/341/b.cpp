// 2024/02/17

#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n;
    cin >> n;
    vector<int> a(n);
    vector<int> s(n), t(n);
    int i;
    for (i = 0; i < n; i++)
    {
        cin >> a.at(i);
    }
    for (i = 0; i < n; i++)
    {
        cin >> s.at(i) >> t.at(i);
    }
}